module.exports = member => {
    let username = member.user.username;
    member.sendMessage('**' + username + '** Welcome To TurkishMP Official Discord Server! Role the language you want from #languages :heart:');

};
